---
'@agentmark-ai/api-types': minor
---

Add an optional `dataType` field to `ScoreAggregation` so the score analytics UI can render type-appropriate aggregation (proportion for boolean, mean for numeric/categorical) and label each score by its persisted data type.
