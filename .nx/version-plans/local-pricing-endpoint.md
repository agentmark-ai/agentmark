---
'@agentmark-ai/cli': minor
---

Implement `GET /v1/pricing` on the local dev server. Serves the bundled `@agentmark-ai/model-registry` pricing snapshot so SDK consumers pointing at `agentmark dev` see the same cost data shape they'd get from cloud.
