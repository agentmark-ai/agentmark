---
'@agentmark-ai/templatedx': minor
---

Add JSON Schema $ref resolution support: resolveSchemaRefs() and resolveAstSchemaRefs() functions for resolving $ref entries in prompt frontmatter schemas at build time, with transitive resolution, JSON Pointer fragment support, and circular reference detection.
