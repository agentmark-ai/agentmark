{/*
  Tool implementations live in TS code (not in MDX frontmatter).
  Frontmatter `tools` is the list of tool names this prompt is allowed to call;
  the actual schemas + handlers are wired up where the AgentMark client is
  constructed (e.g. agentmark.client.ts). See the 2026-03-13 doctrine commit
  "remove tool registries, accept native SDK tools directly".
*/}
---
name: financial-assistant
text_config:
  model_name: anthropic/claude-sonnet-4-20250514
  max_calls: 3
  tools:
    - calculate
    - lookup_conversion_rate
test_settings:
  props:
    question: "If I have 150 EUR, how much is that in USD and GBP?"
input_schema:
  type: object
  properties:
    question:
      type: string
      description: "A question that may require calculation or currency lookup"
  required:
    - question
---

<System>
You are a helpful financial assistant. Use the available tools to answer
questions accurately. Always show your work — explain which tools you called
and what they returned.
</System>

<User>{props.question}</User>
