import { AppBar, Toolbar } from "@mui/material";

export const Header = () => {
  return (
    <AppBar>
      <Toolbar sx={{ height: "100%" }}>Header</Toolbar>
    </AppBar>
  );
};

export default Header;
