---
name: image
image_config:
  model_name: test-model
  num_images: 1
  prompt: "{props.userMessage}"
input_schema:
  type: object
  properties:
    userMessage:
      type: string
  required:
    - userMessage
---

<ImagePrompt>
{props.userMessage}
</ImagePrompt>