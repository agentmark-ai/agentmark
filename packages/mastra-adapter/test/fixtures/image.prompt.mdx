---
name: image
image_config:
  model_name: test-model
  prompt: "Generate an image of {props.userMessage}"
  num_images: 1
input_schema:
  type: object
  properties:
    userMessage:
      type: string
  required:
    - userMessage
---