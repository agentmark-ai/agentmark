---
name: math
object_config:
  model_name: test-model
  temperature: ${temperature:0.5}
  schema:
    type: object
    properties:
      answer:
        type: string
    required: ["answer"]
test_settings:
  props:
    userMessage: "What is 2 + 2?"
  dataset: ${file:./datasets/math.jsonl}
  evals:
   - ${file:./evals/classify.mdx}
   - ${file:./evals/math2.mdx}
input_schema:
  type: object
  properties:
    userMessage:
      type: string
  required:
    - userMessage
---

<System>
You are a helpful math tutor.
</System>

<User>
{props.userMessage}
</User>

<Assistant>
Here's your answer!
</Assistant> 